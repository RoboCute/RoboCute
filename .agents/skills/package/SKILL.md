---
name: package
---

- use [uv](https://github.com/astral-sh/uv) as package manager
- run `uv add <package>` in terminal to install a package
- run `uv run <script>` in terminal to run a python script
- run `uv sync` in terminal to synchronize project's package